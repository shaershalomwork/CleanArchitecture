# Owned domain
Add only application-owned entities, value objects and invariants here.
External rows and payloads belong to Infrastructure. Aggregation models belong to Application.
An empty Domain is intentional. There are no persistence or mediator dependencies.
